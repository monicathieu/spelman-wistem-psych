source("renv/activate.R")
options(blogdown.hugo.dir = here::here("node_modules", ".bin", "hugo"),
        blogdown.hugo.version = "0.101.0",
        # Render to markdown so npm run start handles the actual rendering to html from there
        blogdown.method = "markdown")
